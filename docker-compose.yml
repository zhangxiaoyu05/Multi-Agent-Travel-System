# =============================================================================
# 入境定制游多 Agent 系统——Docker Compose 编排
# =============================================================================
# 启动所有服务：
#   docker-compose up --build -d
#
# 停止：
#   docker-compose down
#
# 服务清单：
#   app              - FastAPI 后端（端口 8000）
#   mysql            - MySQL 8.0（端口 3306）
#   redis            - Redis 7（端口 6379）
#   etcd             - Milvus 元数据协调（端口 2379）
#   minio            - Milvus 对象存储（端口 9000/9001）
#   milvus-standalone- Milvus 向量数据库（端口 19530）
#
# 异地部署：
#   1. 复制项目到目标服务器
#   2. cp .env.example .env 并编辑配置
#   3. docker-compose up --build -d
# =============================================================================

version: "3.8"

services:

  # ===========================================================================
  # FastAPI 应用
  # ===========================================================================
  app:
    build: .
    container_name: travel-agent
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      - MYSQL_HOST=mysql
      - REDIS_HOST=redis
      - MILVUS_HOST=milvus-standalone
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      milvus-standalone:
        condition: service_started
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload
    restart: unless-stopped
    networks:
      - agent_network

  # ===========================================================================
  # MySQL 8.0——会话存储 + LangGraph Checkpoint
  # ===========================================================================
  mysql:
    image: mysql:8.0
    container_name: travel-mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: travel_agent
      MYSQL_USER: travel
      MYSQL_PASSWORD: travel123
    volumes:
      - mysql_data:/var/lib/mysql
      - ./scripts/migrate_mysql.sql:/docker-entrypoint-initdb.d/01_init.sql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "travel", "-ptravel123"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    networks:
      - agent_network

  # ===========================================================================
  # Redis 7——会话缓存 + 摘要缓存
  # ===========================================================================
  redis:
    image: redis:7-alpine
    container_name: travel-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    restart: unless-stopped
    networks:
      - agent_network

  # ===========================================================================
  # etcd——Milvus 元数据协调
  # ===========================================================================
  etcd:
    image: quay.io/coreos/etcd:v3.5.5
    container_name: travel-etcd
    environment:
      - ETCD_AUTO_COMPACTION_MODE=revision
      - ETCD_AUTO_COMPACTION_RETENTION=1000
      - ETCD_QUOTA_BACKEND_BYTES=4294967296
      - ETCD_SNAPSHOT_COUNT=50000
    volumes:
      - etcd_data:/etcd
    command: etcd -advertise-client-urls=http://127.0.0.1:2379 -listen-client-urls http://0.0.0.0:2379 --data-dir /etcd
    healthcheck:
      test: ["CMD", "etcdctl", "endpoint", "health"]
      interval: 30s
      timeout: 10s
      retries: 5
    restart: unless-stopped
    networks:
      - agent_network

  # ===========================================================================
  # MinIO——Milvus 对象存储
  # ===========================================================================
  minio:
    image: minio/minio:RELEASE.2024-01-16T16-07-38Z
    container_name: travel-minio
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - minio_data:/minio_data
    command: minio server /minio_data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 10s
      retries: 5
    restart: unless-stopped
    networks:
      - agent_network

  # ===========================================================================
  # Milvus——向量数据库（单机部署）
  # ===========================================================================
  milvus-standalone:
    image: milvusdb/milvus:v2.4.0
    container_name: travel-milvus
    command: ["milvus", "run", "standalone"]
    environment:
      ETCD_ENDPOINTS: etcd:2379
      MINIO_ADDRESS: minio:9000
      MINIO_ACCESS_KEY_ID: minioadmin
      MINIO_SECRET_ACCESS_KEY: minioadmin
    volumes:
      - milvus_data:/var/lib/milvus
    ports:
      - "19530:19530"
      - "9091:9091"
    depends_on:
      etcd:
        condition: service_healthy
      minio:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9091/healthz"]
      interval: 30s
      timeout: 10s
      retries: 5
    restart: unless-stopped
    networks:
      - agent_network


# =============================================================================
# 持久化卷
# =============================================================================
volumes:
  mysql_data:
  redis_data:
  etcd_data:
  minio_data:
  milvus_data:


# =============================================================================
# 网络
# =============================================================================
networks:
  agent_network:
    driver: bridge
