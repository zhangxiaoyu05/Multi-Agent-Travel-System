# =============================================================================
# 入境定制游多 Agent 系统——Python 依赖
# =============================================================================
# 安装：pip install -r requirements.txt
# 要求：Python ≥ 3.12
# =============================================================================

# ---- LangGraph 编排 ----
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0

# ---- Web 框架 ----
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ---- 数据库 ----
sqlalchemy[asyncio]>=2.0
aiomysql>=0.2.0

# ---- 缓存 ----
redis>=5.0

# ---- 向量数据库（Milvus REST API，零 SDK 依赖） ----
# 使用 httpx 直连 Milvus RESTful API (http://host:19530/api/v1/)
# 无需 pymilvus SDK，避免 gRPC 依赖

# ---- 工具库 ----
pydantic>=2.0
python-dotenv>=1.0
httpx>=0.27
python-jose[cryptography]>=3.3
bcrypt>=4.0

# ---- 测试 ----
pytest>=8.0
pytest-asyncio>=0.24.0
